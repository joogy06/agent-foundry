[pytest]
# By default, skip @pytest.mark.manual tests. Run them explicitly with:
#   pytest -m manual
# or both default + manual:
#   pytest -m "not manual or manual"
addopts = -m "not manual"
markers =
    manual: tests that require manual invocation; skipped in CI / default runs
