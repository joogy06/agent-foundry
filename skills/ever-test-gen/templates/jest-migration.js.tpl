/**
 * EVO-generated migration confirm — CONFIDENCE: {confidence_level}
 * Package: {package} {old_ver} → {new_ver}
 * Breaking change: {breaking_line}
 */

describe("evo migration {component_id} :: {bl_slug}", () => {{
  test.skip("legacy oracle preserved (HARD-RULE 2)", () => {{
    // TODO-IMPLEMENT-ORACLE / CALL / ASSERT
  }});
}});
