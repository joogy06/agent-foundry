{
  "_todo_agy": "verify equivalent — this file encoded the Gemini CLI policy engine (run-gemini-cli policy.schema.json: allowed_tools/denied_tools/sandbox_mode/max_tool_calls). agy (Antigravity CLI 1.0.4) exposes no verified policy-engine/allowlist schema. agy's analogues are coarse-grained CLI flags: --sandbox (restricted-terminal run) and --dangerously-skip-permissions (auto-approve). Until an agy policy-file mechanism is verified, DO NOT fabricate a schema; the bridge must enforce read-only behaviour via agy flags + the workflow-level controls, not via this file.",
  "description": "Bridge closed allowlist policy placeholder for the Antigravity CLI (agy). See references/security-model.md M2. NOTE: the fields below are retained from the retired Gemini CLI policy and have no verified agy analogue (see _todo_agy).",
  "allowed_tools": [],
  "denied_tools": [
    "shell",
    "web_fetch",
    "file_write",
    "file_read",
    "file_edit",
    "file_delete",
    "exec"
  ],
  "sandbox_mode": "plan",
  "max_tool_calls": 0,
  "require_human_approval": false,
  "disable_shell": true,
  "disable_filesystem": true,
  "notes": [
    "TODO(agy): verify equivalent — agy has no confirmed policy-engine. Prefer agy --sandbox + workflow-level scoping to keep v1 kinds (review, research, prompt) read-only.",
    "For v1 kinds (review, research, prompt) agy only needs to generate text responses from the assembled prompt; no tool execution is required.",
    "If a future kind requires tool execution, design an agy-native control (verified) rather than relaxing this placeholder."
  ]
}
