[project]
name = "test-python-vuln"
version = "0.1.0"
description = "Synthetic fixture with a known-vulnerable dep"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.20,<2.28",
    "pyyaml>=6.0",
    "urllib3>=1.26.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "mypy>=1.0",
]
